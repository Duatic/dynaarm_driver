freedrive_controller:
  joints:
    type: string_array
    default_value: []
    description: "Names of joints used by the controller"
    validation:
      unique<>: null
  d_gains:    
    type: double_array
    default_value: [0.1, 0.1, 0.1, 0.01, 0.01, 0.01]
    read_only: true